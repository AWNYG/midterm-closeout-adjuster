# 代码交接与中台接入说明（详细版）

> 适用对象：接手维护 / 对接中台 / 首次阅读本算法的同学。
> 配套文档：仓库根目录《电力滚动撮合收盘微调算法_编写流程.md》（V2.0 定稿，业务规则权威来源）。
> 代码版本：当前定稿实现，205 项测试全绿（含健壮性模糊测试与端到端）。

---

## 目录

1. [项目一句话概括](#1-项目一句话概括)
2. [符号与单位约定](#2-符号与单位约定)
3. [业务模型与结算规则](#3-业务模型与结算规则)
4. [算法详解（含手算示例）](#4-算法详解含手算示例)
5. [模块与函数详解](#5-模块与函数详解)
6. [输入接口规范（逐字段）](#6-输入接口规范逐字段)
7. [输出结果规范（逐字段）](#7-输出结果规范逐字段)
8. [参数配置详解（params.yaml）](#8-参数配置详解paramsyaml)
9. [数据流总览](#9-数据流总览)
10. [中台接入方式](#10-中台接入方式)
11. [运行与验证](#11-运行与验证)
12. [易踩坑清单](#12-易踩坑清单)
13. [已知边界与后续迭代点](#13-已知边界与后续迭代点)
14. [修改指南](#14-修改指南)

---

## 1. 项目一句话概括

**在 D 日滚动撮合收盘前，对 24 个时段（0~23 点）各自的合同持仓做最后一次微调**：
以 D+2 现货价预测 `S` 与负荷预测 `L` 为公平价值锚，计算每个时段合约的边际价值 `MV`，与各时段独立的 5 档盘口对比，为每个时段输出"**买 / 卖 / 不动**"决策——包括**报价区间、目标成交量、初始挂单价**。

执行方式约定：**挂单为主、不追价**，收盘前未成交即放弃该时段。交易员在算法给出的报价区间内人工看盘微调挂单价。

**关键特性**：24 个时段**完全独立**——无全局预算、各自限额、各做一笔交易，互不影响执行顺序。

---

## 2. 符号与单位约定

| 符号 | 含义 | 单位 | 来源 |
|---|---|---|---|
| `t` | 时段编号 0~23（D+2 第 t 个小时） | — | 输入 |
| `Q_c,t`（代码 `Q_c`） | 该时段合同总持仓（聚合） | MWh | 输入 `contract[].volume_mwh` |
| `L_t`（代码 `L`） | 该时段负荷预测 | MWh | 输入 `load_forecast_mwh[t]` |
| `S_t`（代码 `S`） | 该时段实时现货价预测 | 元/MWh | 输入 `spot_forecast_yuan_mwh[t]` |
| `P_avg,c,t`（代码 `P_avg`） | 该时段全部合同的平均批发电价（总电费/总电量） | 元/MWh | 输入 `contract[].avg_price_yuan_mwh` |
| `ask1` / `bid1` | 盘口卖一价 / 买一价 | 元/MWh | 输入 `books[t]` |
| `θ`（theta） | 不交易带半宽（费用+风险溢价） | 元/MWh | 配置 `theta_yuan_mwh` |
| `k` | 回收基准：`k = recover·P_avg − (recover−1)·S` | 元/MWh | 由公式计算 |
| `MV_t(Q)` | 累计买入 Q 个合约单位后，下一单位合约的期望价值 | 元/MWh | 由公式计算 |
| `mv0` | `MV_t(0)`，当前持仓位置第 1 个单位合约的价值 | 元/MWh | `marginal_value()` |
| `min_lot` | 最小交易单位（电量包） | MWh | 输入 `min_lot_mwh` |
| `position_limit` | 单时段持仓上限 | MWh | 输入 `position_limit_mwh` |
| `confidence` | 决策置信度（全局参数，逐时段接入前的占位） | [0,1] | 配置 `confidence` |

**单位约定**：价格一律 **元/MWh**；电量一律 **MWh**；成交量必须是 `min_lot`（10 MWh）的整数倍。

---

## 3. 业务模型与结算规则

### 3.1 偏差考核带（时段级，单时段独立考核）

设某时段：合同持仓 `Q_c`、负荷预测 `L`。定义**偏差带**为负荷与合同的比值区间：

```
带内：   0.85 ≤ L/Q_c ≤ 1.15     （即持仓 Q_c ∈ [L/1.15, L/0.85]）
带外多用：L > 1.15 × Q_c          （负荷比合同多太多，合同不够用）
带外少用：L < 0.85 × Q_c          （合同比负荷多太多，多买了）
```

### 3.2 偏差电量结算价（带外回收）

设 `k = recover·P_avg − (recover−1)·S`（默认 recover=1.1 → `k = 1.1·P_avg − 0.1·S`）：

| 情形 | 偏差电量结算价 | 含义 |
|---|---|---|
| 带内 | `S` | 偏差电量按实时现货价结算，无考核 |
| 带外多用 | `max(S, k)` | 多用且现货低于合同价时，按 `k`（高于现货）结算——"便宜没占成"被放大扣回 |
| 带外少用 | `min(S, k)` | 少用且现货高于合同价时，按 `k`（低于现货）结算——同理对称 |

> **口语化理解**：对用户有利的差价按 1.1 倍回收，形成"带边界回拉"效应——持仓偏离带内越远，合约价值越低，逼着交易员把持仓拉回带内。

### 3.3 合约内在价值 MV（边际价值）

买入第 1 个单位合约（累计量 Q=0）对用户的期望价值：

```
MV_t(0) = 超配 → min(S, k)      （Q_c > L/0.85：合同过剩，合约边际价值低）
          带内 → S              （持仓位置合理，价值=现货价）
          欠配 → max(S, k)      （Q_c < L/1.15：合同不足，合约边际价值高）
```

**MV 是"这个价位以下买入都划算"的价值上限**：
- 欠配时段 MV 高于现货价（因为缺合同会被按 1.1 倍回收惩罚，买合同相当于避免惩罚）；
- 超配时段 MV 低于现货价（多持的合同会被回收，边际价值低）。

### 3.4 成交后混合均价更新

新成交一笔 `ΔQ` 电量的合同、成交价 `p_trade` 后，该时段回收基准变为：

```
P_avg_new = (Q_c·P_avg + ΔQ·p_trade) / (Q_c + ΔQ)
```

`ΔQ=0` 返回原值；`ΔQ ≤ −Q_c`（卖光全部持仓）抛 `ValueError`（均价无意义）。

---

## 4. 算法详解（含手算示例）

单时段决策流程（`decide_period`，24 个时段各调用一次）：

```
步骤1  计算 mv0 = MV_t(0)                    （合约内在价值）
步骤2  方向判定（与 θ 比较）
步骤3  报价区间（买入/卖出对称）
步骤4  目标量（深度 / 带内余量 / 限额取最小，向下取整到 10 MWh）
步骤5  期望收益与留痕
```

### 步骤 1：计算边际价值 `mv0`

```python
mv0 = marginal_value(Q_c, L, S, P_avg, band_low=0.85, band_high=1.15, recover=1.1)
```

- 计算 `k = 1.1·P_avg − 0.1·S`
- `Q_c > L/0.85` → `mv0 = min(S, k)`（超配）
- `Q_c < L/1.15` → `mv0 = max(S, k)`（欠配）
- 其余（带内）→ `mv0 = S`

### 步骤 2：方向判定

```
买：mv0 − θ > ask1      （合约价值扣掉交易成本后仍高于卖一价 → 买）
卖：bid1 − θ > mv0      （买一价扣掉交易成本后仍高于合约价值 → 卖）
其余：hold（不交易）
```

> 两个方向是**互斥**的（一个比一个比价），θ 越大两方向都越难触发 → 越保守。

### 步骤 3：报价区间

```
买：[P_low, P_high] = [bid1, min(ask1, mv0 − θ)]
     P_low = bid1                  # 不抢买：下限不高于买一
     P_high = min(ask1, mv0 − θ)   # 不超过卖一；也绝不超过"内在价值−θ"
卖：[P_low, P_high] = [max(bid1, mv0 + θ), ask1]
     P_low = max(bid1, mv0 + θ)    # 不低于买一；绝不低于"内在价值+θ"
     P_high = ask1                 # 不砸卖：上限不高于卖一
```

交易员在此区间内人工微调挂单价，**不突破区间**。

### 步骤 4：目标量

```
买：q = floor_lot( min(  ask 档中 px ≤ P_high 的深度累计, 带内买入余量, 持仓限额余量 ) )
卖：q = floor_lot( min(  bid 档中 px ≥ P_low 的深度累计, 带内卖出余量, 持仓量 ) )
```

**带内回拉余量**（`band_room`，按"目标位置"口径）：

| 情形 | 目标持仓 | 买入余量 | 卖出余量 |
|---|---|---|---|
| `S < P_avg`（现货便宜→少持合约） | 带下沿 `L/1.15` | `max(0, L/1.15 − Q_c)` | `max(0, Q_c − L/1.15)` |
| `S > P_avg`（合约便宜→多持合约） | 带上沿 `L/0.85` | `max(0, L/0.85 − Q_c)` | `max(0, Q_c − L/0.85)` |

**取整规则**（`floor_lot`）：向下取整到 10 MWh 整数倍，带 1e-9 容差防浮点边界丢整包（如 `299.9999999999999 → 300`）。

**量 < min_lot → 该时段 hold**（方向信号存在但量不足一包）。

### 步骤 5：期望收益与留痕

```
挂单价 = 区间中间价 (P_low + P_high)/2
买：expected_pnl = (mv0 − 挂单价) × 量 × confidence
卖：expected_pnl = (挂单价 − mv0) × 量 × confidence
```

`reasons` 记录决策依据（见 §7.7 标签表），供复盘追责。

### 4.1 手算示例 ①：买入时段（示例输入 t9，欠配）

```
输入：Q_c=150, L=190, S=407.0, P_avg=410.0
盘口：bid1=402.0（12MWh）, ask1=404.0（14MWh）
配置：θ=2.0, min_lot=10, position_limit=250, confidence=0.8
```

| 步骤 | 计算 | 结果 |
|---|---|---|
| 带判定 | L/Q_c = 190/150 = 1.2667 > 1.15 | 带外多用（欠配） |
| k | 1.1×410 − 0.1×407 | **410.3** |
| mv0 | 欠配 → mv0 = max(S, k) | **410.3** |
| 方向 | mv0−θ = 408.3 > ask1=404.0 | **买** |
| 报价区间 | [bid1, min(ask1, 408.3)] = [402.0, 404.0] | **[402.0, 404.0]** |
| 深度 | ask 档 px ≤ 404.0：仅第 1 档 → 14 | 14 |
| 带内余量 | S=407 < P_avg=410 → 目标 L/1.15=165.2；买入余量=165.2−150 | 15.2 |
| 限额余量 | 250 − 150 | 100 |
| 目标量 | min(14, 15.2, 100) → 取整 | **10** |
| 挂单价 | (402.0+404.0)/2 | **403.0** |
| 期望收益 | (410.3 − 403.0) × 10 × 0.8 | **58.40 元** |
| reasons | 触发买、量=深度 | `["spread>theta","depth_cap"]` |

### 4.2 手算示例 ②：卖出时段（示例输入 t0）

```
输入：Q_c=130, L=128, S=332.0, P_avg=337.5
盘口：bid1=336.0（12MWh）, ask1=338.0（14MWh）
配置：θ=2.0, min_lot=10, position_limit=250, confidence=0.8
```

| 步骤 | 计算 | 结果 |
|---|---|---|
| 带判定 | L/Q_c = 128/130 = 0.9846 ∈ [0.85, 1.15] | 带内 |
| mv0 | 带内 → mv0 = S | **332.0** |
| 方向 | bid1−θ = 334.0 > mv0=332.0 | **卖** |
| 报价区间 | [max(336.0, 334.0), 338.0] = [336.0, 338.0] | **[336.0, 338.0]** |
| 深度 | bid 档 px ≥ 336.0：仅第 1 档 → 12 | 12 |
| 带内余量 | S=332 < P_avg=337.5 → 目标 L/1.15=111.3；卖出余量=130−111.3 | 18.7 |
| 目标量 | min(12, 18.7, 130) → 取整 | **10** |
| 挂单价 | (336.0+338.0)/2 | **337.0** |
| 期望收益 | (337.0 − 332.0) × 10 × 0.8 | **40.00 元** |
| reasons | 触发卖、量=深度 | `["spread>theta","depth_cap"]` |

> 两个示例与 `python demo.py` 的实际输出完全一致，可用于联调核对。

### 4.3 两个关键实现推论（勿"修正"回去）

1. **买报价上界恒 = ask1、卖报价下界恒 = bid1**：买方向触发条件 `mv0−θ > ask1` 数学上蕴含 `ask1 < mv0−θ`，因此 `min(ask1, mv0−θ)` 恒等于 ask1；卖对称。MV±θ 封顶只在触发边界恰好相等时才有意义。
2. **深度只统计第 1 档**：由推论 1，区间上界=ask1 时只有 px≤ask1 的档位（即 ask1 档本身）；卖同理。所以"区间内对手方档位深度累计"实际等于 **ask1/bid1 档的量**。

---

## 5. 模块与函数详解

### 5.1 模块依赖

```
settlement.py ← value.py ← decision.py ← main.py
                  risk.py  ← (被 decision 与 main 引用)
ingestion.py  ← main.py
```

`main` 是唯一有副作用（写文件）的模块；其余均为纯函数/数据结构，可直接单测。

### 5.2 settlement.py — 结算模型（纯函数）

| 函数 | 签名 | 行为 | 异常 |
|---|---|---|---|
| `settle_price` | `(Q_c, L, S, P_avg, band_low=0.85, band_high=1.15, recover=1.1) → float` | 带内返 S；多用 `max(S,k)`；少用 `min(S,k)` | Q_c ≤ 0 → `ValueError` |
| `marginal_value` | `(Q_c, L, S, P_avg, ..., shrink=0.0) → float` | 超配 `min(S,k)`；欠配 `max(S,k)`；带内 S | Q_c ≤ 0 → `ValueError` |
| `update_blended_avg` | `(Q_c, P_avg, delta_q, p_trade) → float` | 混合均价公式 | ΔQ ≤ −Q_c → `ValueError` |

### 5.3 value.py — MV 阶梯曲线（numpy）

| 函数 | 签名 | 行为 |
|---|---|---|
| `band_bounds` | `(load, ..., shrink=0.0) → (lo, hi)` | 返回 `[L/1.15, L/0.85]`；shrink 按比例向中心收缩 |
| `mv_curve` | `(Q_c, L, S, P_avg, max_q, step=1.0, ...) → (qs, mv)` | Q 从 0 到 max_q 的 MV 阶梯，单调不增；跨带边界处出现阶梯 |

`mv_curve` 目前仅测试使用（验证单调性与阶梯位置）；生产决策用 `marginal_value` 单点。

### 5.4 decision.py — 单时段决策（核心）

`Decision` dataclass（字段见 §7.2 输出表）。三个函数：

- `band_room(Q_c, L, S, P_avg, band_low, band_high, shrink) → (room_buy, room_sell)`：带内回拉余量（§4 步骤 4）。
- `_initial_price(p_low, p_high, policy) → float`：初始挂单价 = 区间中间价（策略扩展点）。
- `decide_period(book, Q_c, L, S, P_avg, position_limit, min_lot, theta, confidence=1.0, period=0, band_low, band_high, recover, shrink, initial_order_policy) → Decision`：完整单时段决策（§4 步骤 1~5）。

注意：**单时段金额上限不在本层**，由调用方经 `risk.check` 裁剪。

### 5.5 risk.py — 风控

| 函数 | 签名 | 行为 |
|---|---|---|
| `floor_lot` | `(volume, min_lot) → float` | 向下取整到 min_lot 整数倍；负量→0；NaN→`ValueError`；1e-9 容差 |
| `check` | `(volume, position, position_limit, max_amount, best_price, confidence, min_lot, confidence_min=0.5, side="buy") → (volume, reasons)` | ① 置信度 < min → 否决（NaN 同样否决）；② **仅 `side="buy"` 受持仓限额约束**（卖是减仓，超限可卖）；③ 金额上限；④ 取整；⑤ 裁剪时追加 `position_cap`/`amount_cap` 标签 |

### 5.6 ingestion.py — 输入校验

- `_finite_number(x, name, allow_negative)`：数值必须是有限 int/float（拒绝 NaN/Inf/字符串/None/布尔）。
- `_check_levels(levels, descending, name, price_min, price_max)`：恰 5 档、有序、量 ≥ 0、档位价在 `[price_min, price_max]`。
- `validate(data, price_min=0, price_max=9999, spot_price_min=None, spot_price_max=None)`：全量校验（详见 §6.3），失败抛 `ValidationError`（`ValueError` 子类）。
- `fetch_books()`：爬虫占位，返回 24 盘口列表；未实现返回 None 走本地输入。

### 5.7 main.py — 执行入口

- `load_config(path)`：PyYAML 读配置。
- `run(config, data, out_dir) → dict`：**中台主入口**（详见 §9）。配置数值经 `_cfg_number` 类型校验（拒绝字符串/NaN/Inf/越界；含 `confidence∈[0,1]`、`shrink_bands∈[0,1)`、`band_low < band_high`）→ 校验 → 24 时段循环（decide_period → check → 组装 dict）→ 写 `<交割日>-decision.json` → 返回结果 dict。
- `print_summary(result)`：控制台按价差降序打印决策表（交易员执行参考）。
- `main(argv)`：CLI 入口，`--config/--input/--out` 覆盖默认路径；全部异常（含 yaml 语法错误、配置类型非法）友好输出并返回非 0。

### 5.8 reasons 标签详解（留痕字段）

`reasons` 是决策的留痕标签数组，全部在 `decision.py` / `risk.py` 中产生，用于复盘、追责与调参。共 8 种：

| 标签 | 产生位置 | 触发条件 | 含义 | 典型组合 |
|---|---|---|---|---|
| `spread>theta` | decision 买/卖分支 | `mv0−θ > ask1` 或 `bid1−θ > mv0` | 价差超过不交易带，有交易信号 | 所有非 hold 决策都以此开头 |
| `depth_cap` | decision 买/卖分支 | 目标量 = 区间内对手方深度（三方取最小且深度最小） | 想买/卖更多但盘口第 1 档量不够 | `spread>theta,depth_cap` |
| `band_edge` | decision 买/卖分支 | 目标量 = 带内回拉余量 | 量被"拉回带内"的余量封顶，已到目标持仓位置 | `spread>theta,band_edge` |
| `position_cap` | decision 买分支 / risk.check | 买单限额余量为三方最小值；限额余量 ≤ 0 → hold | 量受持仓限额约束，**仅买单**（卖单减仓不受限） | `spread>theta,position_cap` |
| `amount_cap` | risk.check | 按 `max_amount_yuan_mwh` 金额上限裁剪 | 量受单时段金额上限约束 | `spread>theta,amount_cap` |
| `below_min_lot` | decision 买/卖分支 | 方向触发但目标量取整后 < min_lot | 信号存在但量不足一包 → hold（保留触发原因） | `spread>theta,below_min_lot` |
| `hold_no_spread` | decision 兜底 | 方向判定不通过（价差 ≤ θ） | 无足够价差，直接不动 | 单独出现（纯 hold） |
| `low_confidence` | risk.check | 置信度 < `confidence_min` → 量裁为 0 | 预测置信度不足，否决交易 | `spread>theta,low_confidence` |

**阅读 reasons 的三个要点**：

1. **hold ≠ 无信号**。方向触发后被风控拦住的 hold，reasons 保留原始信号 + 拦截原因（如 `spread>theta,below_min_lot`、`spread>theta,position_cap`、`spread>theta,low_confidence`）——复盘可区分"曾有信号但没成交"与"本就没信号"（纯 `hold_no_spread`）。
2. **`depth_cap` / `band_edge` / `position_cap` 互斥**。目标量 = min(深度, 带内余量, 限额余量) 三方取最小，谁最小打谁的标签；若两个值相等且并列最小则同时出现。
3. **`position_cap` 只在买方向**。卖单减仓不受持仓限额约束（超限持仓允许卖出），卖方向永远不出现该标签。

> 实际组合示例（`data/output/2026-08-27-decision.json`）：t8 买 10 MWh → `spread>theta,depth_cap`（量=盘口第 1 档深度）；t1 → `hold_no_spread`（无信号）。

---

## 6. 输入接口规范（逐字段）

### 6.1 完整 JSON 示例（带逐字段注释）

```json
{
  "as_of": "2026-08-20T14:55:00+08:00",
  "delivery_date": "2026-08-22",

  "spot_forecast_yuan_mwh": [332.0, 338.0, 340.0, "…共 24 个点"],
  "load_forecast_mwh":      [128,   124,   116,   "…共 24 个点"],

  "contract": [
    {"period": 0, "volume_mwh": 130, "avg_price_yuan_mwh": 337.5},
    "…共 24 个时段，period 必须等于列表下标"
  ],

  "books": [
    {
      "bid": [{"px": 336.0, "vol": 12}, "…共 5 档，价格递减"],
      "ask": [{"px": 338.0, "vol": 14}, "…共 5 档，价格递增"]
    },
    "…共 24 个时段，每个时段独立盘口"
  ],

  "position_limit_mwh": 250,
  "min_lot_mwh": 10
}
```

### 6.2 字段逐项解释

#### `as_of`（字符串，必填）
- **含义**：盘口快照时刻（爬取时间），ISO8601 格式，建议带时区（`+08:00`）。
- **用途**：仅回显到输出；不参与计算。
- **校验**：必须是非空字符串（不校验格式）。
- **接入要点**：24 个盘口必须是**同一时刻**的快照，快照过旧应重新爬取再决策。

#### `delivery_date`（字符串，必填）
- **含义**：交割日（D+2），格式 `YYYY-MM-DD`，如 `2026-08-22`。
- **用途**：决定输出文件名 `data/output/<delivery_date>-decision.json`。
- **校验**：必须匹配 `^\d{4}-\d{2}-\d{2}$`（**防路径注入**——不允许 `/`、`\`、`..` 等字符）。

#### `spot_forecast_yuan_mwh`（float 数组，长度 24，必填）
- **含义**：24 点实时现货价预测，下标 = 时段 t。
- **单位/范围**：元/MWh；`spot_price_min_yuan_mwh ≤ 值 ≤ spot_price_max_yuan_mwh`（默认 -50 ~ 800）。**现货可负**（现货价格不受中长期 20% 上浮限制）；负现货价时全链路行为已测试锁定（带外结算/方向判定/期望收益，见 `TestNegativeSpot`）。
- **用途**：公平价值锚——决定 MV 与报价区间。
- **校验**：长度必须 24；每个值必须是有限数值（拒绝 NaN/Inf/字符串/None）。

#### `load_forecast_mwh`（float 数组，长度 24，必填）
- **含义**：24 点 D+2 负荷预测（用户预测实际用电），下标 = 时段 t。
- **单位/范围**：MWh；≥ 0。
- **用途**：与合同持仓比较得出偏差带位置，决定 MV 与带内回拉余量。
- **校验**：长度 24；有限数值且 ≥ 0。

#### `contract`（dict 数组，长度 24，必填）
- **含义**：每时段**聚合**形态的合同信息（总电量 + 加权均价），不是逐笔合同。
- **字段**：
  - `period`：时段编号 0~23，**必须等于列表下标**（升序排列）。
  - `volume_mwh`：该时段全部生效合同电量之和 `Q_c`（MWh），> 0。
  - `avg_price_yuan_mwh`：该时段全部合同平均批发电价 `P_avg` = 总电费/总电量（元/MWh），须在 `[price_min, price_max]`（默认 [0, 481.44]）内。
- **用途**：计算偏差带位置（Q_c 与 L 比）与回收基准（P_avg 决定 k）。
- **校验**：恰 24 条；period 集合 = 0~23 且与下标一致；电量 > 0；均价有限且在限价内。

#### `books`（dict 数组，长度 24，必填）
- **含义**：每个时段独立的 5 档盘口。
- **字段**：
  - `bid`：5 个买档 `{"px": 价格, "vol": 量}`，**价格严格递减**（bid1 最高）。px 单位元/MWh，vol 单位 MWh。
  - `ask`：5 个卖档 `{"px": 价格, "vol": 量}`，**价格严格递增**（ask1 最低）。
- **约束**：bid1 < ask1（不能交叉）；每档 vol ≥ 0（0 深度合法）；档位价格在 `[price_min, price_max]`（默认 [0, 481.44]）内。
- **用途**：方向判定（ask1/bid1）、报价区间上下界、深度封顶（目标量）。

#### `position_limit_mwh`（float，必填）
- **含义**：单时段持仓上限（风控），只约束**买入**后持仓不超限；卖出（减仓）不受限。
- **校验**：有限正数。

#### `min_lot_mwh`（float，必填）
- **含义**：最小交易单位（电量包）。生产 = 10 MWh，成交量必须为其整数倍。
- **校验**：有限正数。

### 6.3 校验规则汇总（validate）

| 类别 | 规则 |
|---|---|
| 结构 | 顶层必须为 JSON 对象（dict）；8 个字段齐全 |
| 时间 | `as_of` 为字符串；`delivery_date` 匹配 `YYYY-MM-DD` |
| 预测 | 现货/负荷各 24 点；现货 ∈ [-50, 800]（可负）；负荷 ≥ 0；均须有限数值 |
| 合同 | 24 条；period 0~23 且与下标一致；电量 > 0；均价 ∈ [0, 481.44] |
| 盘口 | 24 个；bid 5 档递减 / ask 5 档递增；bid1 < ask1；档位价 ∈ [0, 481.44]；量 ≥ 0 |
| 其他 | position_limit、min_lot 为正 |

**校验失败行为**：抛 `ValidationError`（`ValueError` 子类），**中止整个决策**，不产出半成品。接入方必须保证数据合法。

---

## 7. 输出结果规范（逐字段）

### 7.1 完整 JSON 示例

```json
{
  "as_of": "2026-08-20T14:55:00+08:00",
  "decision_time": "2026-08-20T14:55:01+08:00",
  "delivery_date": "2026-08-22",
  "decisions": [
    {
      "period": 0,
      "action": "sell",
      "price_range": [336.0, 338.0],
      "volume_mwh": 10,
      "orders": [{"side": "sell", "price": 337.0, "volume": 10}],
      "mv": 332.0,
      "expected_pnl_cny": 40.0,
      "reasons": ["spread>theta", "depth_cap"]
    },
    "…共 24 个时段"
  ],
  "summary": {
    "buy_total_mwh": 80,
    "sell_total_mwh": 50,
    "hold_count": 11,
    "confidence": 0.8
  }
}
```

### 7.2 字段逐项解释

#### 顶层

| 字段 | 类型 | 含义 |
|---|---|---|
| `as_of` | str | 原样回显输入快照时刻 |
| `decision_time` | str | 决策生成时刻（本机时区 ISO8601，精确到秒） |
| `delivery_date` | str | 原样回显交割日 |
| `decisions` | list[dict] | 24 个时段的决策，按下标对应时段 |
| `summary` | dict | 汇总统计 |

#### decisions[]. 每时段决策

| 字段 | 类型 | 示例 | 含义与使用方式 |
|---|---|---|---|
| `period` | int | 0 | 时段编号 0~23（= 列表下标） |
| `action` | str | `"sell"` | `buy` / `sell` / `hold`。**交易员只看非 hold 时段** |
| `price_range` | list[float] \| null | `[336.0, 338.0]` | **报价区间 [低, 高]**：买 = `[bid1, min(ask1, mv0−θ)]`；卖 = `[max(bid1, mv0+θ), ask1]`。交易员在此区间内挂单/微调，**不突破**；hold 为 `null` |
| `volume_mwh` | float | 10 | **目标成交量**，min_lot（10）的整数倍；= min(区间内深度, 带内回拉余量, 限额余量) 取整；hold 为 0 |
| `orders` | list[dict] | `[{"side":"sell","price":337.0,"volume":10}]` | **初始挂单建议**（区间中间价 × 目标量）。中台可直接按此下单或展示给交易员；hold 为 `[]` |
| `mv` | float | 332.0 | **MV_t(0) 估值**——该时段合约内在价值（元/MWh），复盘基准 |
| `expected_pnl_cny` | float | 40.0 | **期望收益** = (挂单价与 mv 之差) × 量 × confidence（保守口径，元）；hold 为 0 |
| `reasons` | list[str] | `["spread>theta","depth_cap"]` | **决策留痕标签**（§5.8），复盘/追责/调参依据 |

> **消费指引（交易员）**：对每个非 hold 时段——按 `orders[0]` 挂单；每隔几秒看盘，在 `price_range` 内微调价格促成成交；收盘前未成交即放弃该时段，不追价、不突破区间。

#### summary 汇总

| 字段 | 类型 | 含义 |
|---|---|---|
| `buy_total_mwh` | float | 全部时段买入量之和 |
| `sell_total_mwh` | float | 全部时段卖出量之和 |
| `hold_count` | int | hold 时段数（buy+sell 时段数 = 24 − hold_count） |
| `confidence` | float | 本次决策使用的置信度（来自 config） |

### 7.3 输出文件

- 路径：`data/output/<delivery_date>-decision.json`
- 编码：UTF-8 无 BOM，`ensure_ascii=False`（中文可读）。
- 幂等性：同输入同输出，仅 `decision_time` 不同。

---

## 8. 参数配置详解（params.yaml）

### 8.1 参数表

| 参数 | 默认 | 作用于哪一步 | 改了会怎样 | 调参建议 |
|---|---|---|---|---|
| `deviation_band_low` | 0.85 | 结算/MV/余量 | 带边界移动，改变"带内/带外"判定 | 随考核细则调整 |
| `deviation_band_high` | 1.15 | 结算/MV/余量 | 同上 | 随考核细则调整 |
| `recover_factor` | 1.1 | 结算/MV | `k = recover·P_avg − (recover−1)·S` 改变，影响欠配/超配时段 MV 与方向 | 随回收规则调整 |
| `theta_yuan_mwh` | 2.0 | 方向/区间 | 越大越保守（更难触发交易） | **无回测，先保守；复盘 20+ 笔后重标定** |
| `min_lot_mwh` | 10 | 取整 | 改变量粒度（**由输入 JSON 提供**，yaml 不重复定义） | 按平台最小电量包 |
| `position_limit_mwh` | 250（样例） | 风控 | 约束买单上限；= **合同容量×1h**（**由输入 JSON 提供**，yaml 不重复定义） | 按风控制度/合同容量 |
| `max_amount_yuan_mwh` | null | 风控 | 启用后单时段金额封顶（量 = 金额/挂单价 向下取整） | 可选 |
| `execution_mode` | "limit_order" | 说明性 | 不参与计算（文档记录执行方式） | — |
| `initial_order_policy` | "mid" | 挂单价 | 决定初始挂单价（当前仅 mid） | 新增策略改 `_initial_price` |
| `confidence` | 0.8 | 期望收益/风控 | 缩放 expected_pnl；低于 confidence_min 直接 hold | 预测系统逐时段接入前用全局值 |
| `confidence_min` | 0.5 | 风控 | 调高 → 更容易直接 hold | 按可接受的弃单率 |
| `confidence_shrink` | true | 收缩开关 | false 时 shrink_bands 强制为 0 | — |
| `shrink_bands` | 0.0 | MV/余量 | >0 时带边界向中心收缩（保守化），方向更难触发、量更小 | σ 不可用时的保守参数 |
| `sigma_load_mwh` / `sigma_spot_yuan_mwh` | null | 预留 | **未接入计算**（未来按 ±k·σ 收缩） | 接入预测误差后再启用 |
| `price_min/max_yuan_mwh` | 0 / 481.44 | 校验 | 盘口/合同价格合法性范围（481.44 = 四川燃煤基准价 401.2 × 1.2，发改价格〔2021〕1439号） | 随中长期限价调整 |
| `spot_price_min/max_yuan_mwh` | -50 / 800 | 校验 | 现货价预测合法性范围（现货不受 20% 限制、可负） | 随现货限价调整 |

### 8.2 输入 JSON 与 params.yaml 的取值边界

- **`min_lot_mwh`、`position_limit_mwh` 由输入 JSON 提供**（每日/每市场动态值），params.yaml 不重复定义——改 params.yaml 对它们无效。
- 其余算法参数（θ、考核带、recover、置信度、收缩、限价等）全部在 params.yaml，已接入决策链并有回归测试防"死配置"。
- 配置数值经类型校验（`_cfg_number`）：字符串/NaN/Inf/越界抛 `ValueError`，CLI 层统一捕获为友好错误。

---

## 9. 数据流总览

```
输入 JSON（§6.1 结构）
    │
    ▼
load_input()         读文件 → dict
    │
    ▼
validate()           严格校验（§6.3），任一问题抛 ValidationError → 中止
    │
    ▼
run(config, data)    主循环，for t in 0..23：
    │   ① decide_period(...)    → 单时段 Decision（§4 步骤 1~5）
    │   ② check(...)            → 风控裁剪（置信度/限额/金额/取整）
    │   ③ 组装 decisions[t]（风控后量变化时按比例缩放 expected_pnl）
    ▼
写 data/output/<交割日>-decision.json（UTF-8）
    ▼
print_summary()      控制台按价差降序打印（交易员执行参考）
```

**风控后处理细节**：`check` 把量裁到 0 时，该时段动作转为 `hold`，但 reasons 保留原始信号（如 `spread>theta`）+ 裁剪原因——复盘能看出"曾有信号但被风控拦住"。

**中台对接点**：`run(config, data, out_dir) -> dict` 是完整的"输入 dict → 输出 dict"纯调用，调用一次即得全部决策；文件读写只是留痕副产物。

---

## 10. 中台接入方式

### 10.1 推荐：Python 库调用（进程内）

```python
import sys
sys.path.append("electricity_rolling_trader")   # 或安装为包
from src.main import run, load_config
from src.ingestion import load_input, ValidationError

config = load_config("config/params.yaml")
data = load_input("data/input/2026-08-22.json")   # 或由中台自行组 dict

try:
    result = run(config, data, out_dir="data/output")
except ValidationError as e:
    handle_reject(e)          # 输入脏数据 → 记录告警，不下单
except ValueError as e:
    handle_config_error(e)    # 配置非法 → 修复配置

for d in result["decisions"]:
    if d["action"] == "hold":
        continue
    submit_order(d["period"], d["orders"][0])     # 交易员/自动下单
```

要点：
- `run()` **无状态、幂等**（同输入同输出，仅 `decision_time` 不同）。
- 校验失败抛 `ValidationError`，**不会返回半成品**。
- 决策过程不触发任何外部调用（`fetch_books` 未启用），可完全离线联调。

### 10.2 文件接口（现成）

- 输入落盘 `data/input/<交割日>.json` → `python -m src.main`（自动取最新文件）
- 输出读取 `data/output/<交割日>-decision.json`

### 10.3 HTTP 服务化（建议封装薄层）

```
POST /api/rolling-decision
  body:  <§6.1 输入 JSON>
  resp:  200 → <§7.1 输出 JSON>
         400 → {"error": "<ValidationError 信息>"}
         422 → {"error": "<配置错误>"}
```

单次运行 < 10ms（纯内存 + numpy），无需缓存。

### 10.4 中台侧需准备的数据源

| 数据 | 来源 | 时点要求 | 对应输入字段 |
|---|---|---|---|
| 24 时段 5 档盘口 | 交易平台实时行情 | 收盘前**同一时刻**快照 | `books` |
| D+2 现货价预测（24 点） | 预测系统 | 收盘前 | `spot_forecast_yuan_mwh` |
| D+2 负荷预测（24 点） | 预测系统 | 收盘前 | `load_forecast_mwh` |
| 各时段合同总电量 + 加权均价 | 内部台账/结算系统 | 聚合形态（非逐笔） | `contract` |
| 单时段持仓限额 | 风控 | 静态 | `position_limit_mwh` |

---

## 11. 运行与验证

```bash
cd electricity_rolling_trader
python -m pytest tests/ -q          # 205 项全量测试
python demo.py                      # 终端演示（决策总表 + 逐时段盘口明细）
python -m src.main --input data/sample/2026-08-22.json   # 端到端
```

测试布局：
- `test_settlement.py`：带边界（r=0.85/1.15 精确覆盖）、回收四象限、混合均价。
- `test_value.py`：MV 单调性、阶梯位置、shrink 收缩。
- `test_decision.py`：区间恒不等式、深度/限额封顶、最小单位、below_min_lot/position_cap 留痕。
- `test_risk.py`：裁剪（含卖单不受限额）、否决（含 NaN 置信度）、取整。
- `test_ingestion.py`：脏数据拒绝（缺字段/乱序/越界/交叉盘口）。
- `test_robustness.py`：NaN/Inf/类型注入、分限价校验（现货 [-50,800] vs 盘口 [0,481.44]）、800+ 轮确定性模糊、CLI 错误路径。
- `test_e2e.py`：示例输入 → 24 decisions + summary；配置参数接线回归（monkeypatch 捕获 kwargs）。

---

## 12. 易踩坑清单

1. **带口径**：考核带是 **L/Q ∈ [0.85, 1.15]**（边界 L/1.15、L/0.85），不是 (L−Q)/Q。文档 2.1 偏差率式与"多用 L>1.15Q"表述矛盾，实现统一采用 L/Q 带；改结算规则时 settlement/value/decision 三个模块必须同口径。
2. **卖单不受持仓限额约束**：`check(side="sell")` 不裁剪；别"顺手"把卖单限制在 `position_limit − Q_c`，否则超限持仓永远无法减仓。
3. **深度只数第 1 档**：严格触发下 P_high=ask1 / P_low=bid1（§4.3 推论），"区间内深度累计"实际是第 1 档深度。若未来放宽触发条件，需同步重写深度统计与测试。
4. **输入校验是硬门槛**：校验失败直接抛错中止，**不会**自动修数据或降级。
5. **period 必须与列表下标一致**：contract/books 均按下标访问，乱序会被 validate 拒绝。
6. **限价双轨**：现货预测按 [-50, 800] 校验（可负），盘口/合同按 [0, 481.44] 校验（中长期 20% 上浮）——别用错配置。
7. **Python 版本**：代码用 `X | None` 与 dataclass，需 Python ≥ 3.10；numpy 仅 value.py 使用。
8. **decision_time 时区**：取本机时区；部署环境时区应与北京时间一致（或展示层统一转 +08:00）。
9. **θ 无回测**：θ=2.0 是保守默认值，按复盘记录迭代，勿直接抄到其他市场/品种。
10. **风险前置校验（ingestion）与决策层（decision）各自独立的浮点容差**：`floor_lot` 的 1e-9 容差是防 `L/1.15−Q_c` 类计算丢整包，改 min_lot 或带参数时勿删。

---

## 13. 已知边界与后续迭代点

| 项 | 现状 | 后续 |
|---|---|---|
| `mv_curve` | 完整 MV 阶梯已实现（numpy），生产只用 Q=0 单点 | 如需"多档逐档吃单"决策，启用阶梯与逐笔混合均价更新 |
| `sigma_*` | 参数预留未接入 | 接入预测误差 σ 后按 ±k·σ 收缩带边界 |
| `fetch_books` | 占位返回 None | 对接行情爬虫提供 24 盘口实时快照 |
| `initial_order_policy` | 仅 mid | 扩展"贴卖/贴买"策略，改 `_initial_price` |
| `unfilled/partial/filled` | 输出未含 | 中台下单回执后回写 status 用于复盘 |
| 结算细则版本 | 按 L/Q 带实现（V2.0 定稿） | 规则更新时先改 params.yaml，再评估代码口径 |
| `confidence` 全局值 | 单一全局置信度 | 预测系统提供逐时段置信度后改为 24 点数组 |

---

## 14. 修改指南（接到需求改哪里）

| 需求 | 修改点 |
|---|---|
| 调保守度（手续费/风险溢价） | `params.yaml: theta_yuan_mwh` |
| 改考核带/回收倍数 | `params.yaml: deviation_band_*`、`recover_factor` + 确认三模块同口径 |
| 改最小交易单位 | 输入 JSON `min_lot_mwh`（与 yaml 一致） |
| 改持仓限额 | 输入 JSON `position_limit_mwh` |
| 改限价区间 | `params.yaml: price_*`、`spot_price_*` |
| 改报价区间规则 | `decision.py: decide_period`（§4 步骤 3） |
| 改目标量规则 | `decision.py`（depth/band_room/限额组合，§4 步骤 4） |
| 改混合均价更新 | `settlement.py: update_blended_avg` |
| 改输入格式 | `ingestion.py: validate` + `main.py: run` + 样例与测试 |
| 新增初始挂单策略 | `decision.py: _initial_price` |
| 新增 reasons 标签 | `decision.py` / `risk.py` 追加，同步本文 §5.8 |
| 接入逐时段置信度 | 输入加 confidence 数组 + `main.run` 读取 + `decide_period` 透传 |
